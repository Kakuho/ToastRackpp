namespace dism{
  // the idea is the disassembler does table look ups per instruction
  // these are the following tables we need to implement:
  //  Unprefixed opcodes
  //  CB‑prefixed opcodes
  //  ED‑prefixed opcodes
  //  DD‑prefixed opcodes
  //  FD‑prefixed opcodes
  //  DDCB/FDCB‑prefixed opcodes
  class driver{
    
  };
}
